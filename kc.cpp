#include<bits/stdc++.h>
using namespace std;
algo nextval(k){
  while(true){
    x[k] = x[k]+1mod(m+1);
    if(x[k]==0) return;
    for(j=i to n ) do 
    if(G(j,k)!=0 && x[j]==x[k]) then
    break;
      
    if(j==n+1) return;
      
    
  }
}
alogo mcolor(k){
  while(true){
    nextval(k);
    if(x[k]==0)) then return;
    if(k==n) then 
    write(x[1:n]);
    else mcolor(k);
  }
}
int main(){
  int num = "12234343";

 return 0;
}