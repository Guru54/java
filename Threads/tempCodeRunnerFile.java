  // ArrayList<Integer>  listi = Arrays.asList(5,32,43,43,22,24,12,21);
        // // even list 
        // ArrayList<Integer> evenList = list.stream()
        //     .filter(i -> i % 2 == 0)
        //     .collect(Collectors.toCollection(ArrayList::new));