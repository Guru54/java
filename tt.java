// hello world 

class tt {

    public static void main(String[] args) {
        // String str = "Helloii";
        // String n = "";
//         char ch;
//         for (int i = 0; i < str.length(); i++) {
//             ch = str.charAt(i);
//             if (n.indexOf(ch) == -1) { // Append only if 'ch' is not already in 'n'
//                 n += ch;
//             }
//         }

//         System.out.println("Hello, World! " + n);

//         String c = "";
//         for (int i = 0; i < str.length() - 1; i++) {
//             char a = str.charAt(i);
//             char b = str.charAt(i + 1);
//             if (a == b) {
//                 c += a;
//                 System.out.println("the first repeating element is : " + c);
//                 break;

//             }
//         }
//         String st = "helloiis";
//         int idx = 0;

//         for (int i = 0; i < st.length() - 1; i++) {
//             char a = st.charAt(i);
//             char b = st.charAt(i + 1);
//             if (a == b) {
//                 idx = i;
//                 while (a == b) {
//                     i++;
//                     a = st.charAt(i);
//                     b = st.charAt(i + 1);
//                     idx++;
//                 }
//                 break;
//             }
//         }
//         for (int i = idx; i < st.length() - 1; i++) {
//             char a = st.charAt(i);
//             char b = st.charAt(i + 1);
//             if (a == b) {
//                 System.out.print("the second repeating char is : " + a);
//                 break;
//             }
//         }

// // priting the char repeating most
// int cnt =0;
// char []arr = st.toCharArray();

//         for (int i = 0; i < st.length(); i++) {
//           System.err.print(arr[i]+ " ");
//         }
int a = 5;
int b = a++ + ++a;

  }
}

